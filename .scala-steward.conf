pullRequests.grouping = [
  { name = "all", "title" = "Dependency updates", "filter" = [{"group" = "*"}] }
]

updates.cooldown = {
  minimumAge: "14 days"
}

pullRequests.frequency = "14 days"

updates.pin = [
  { groupId = "org.scala-lang", artifactId = "scala3-library", version = "3.9." }
  { groupId = "org.scala-lang", artifactId = "scala3-compiler", version = "3.9." }
  { groupId = "org.scala-lang", artifactId = "scala3-sbt-bridge", version = "3.9." }
]
